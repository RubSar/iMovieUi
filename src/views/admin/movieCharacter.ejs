<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="stylesheet" href="/lib/bootstrap/dist/css/bootstrap.css"/>
    <link rel="stylesheet" href="/lib/font-awesome/css/font-awesome.min.css"/>
</head>

<body id="body" data-spy="scroll" data-target=".one-page-header" class="demo-lightbox-gallery">
<main class="wrapper">
    <header>
        <% include partials/header.ejs %>
    </header>


    <div class="container" style="margin-top: 60px;">
        <div class="jumbotron">
            <%if (locals.errorMessage){%>
            <div class="text-center">
                <span class="alert-danger"> <%= errorMessage%> </span>
            </div>
            <%}%>
            <h1> Create new Movie Character</h1>
            <% var name = locals.name? locals.name : ''%>
            <% var playedBy = locals.playedBy? locals.playedBy : ''%>
            <form action="/admin/movieCharacter/create" method="post">
                <div class="row form-group">
                    <div class="col-md-6">
                        <input type="text" name="name" value="<%= name%>" class="form-control" placeholder="Name"/>
                    </div>
                    <div class="col-md-6">
                        <input type="text" name="playedBy" value="<%= playedBy%>" class="form-control" placeholder="Played By"/>
                    </div>


                </div>
                <div class="form-group">
                    <input type="text" name="movie" class="form-control" placeholder="Movie name"/>
                </div>

                <div class="form-group">
                    <button type="submit" class="btn btn-primary">Submit</button>
                </div>
            </form>

        </div>
    </div>

</main>

</body>
